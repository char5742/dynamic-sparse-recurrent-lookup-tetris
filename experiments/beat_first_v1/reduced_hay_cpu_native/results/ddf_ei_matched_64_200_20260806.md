# DDF E/I-calibrated matched-optimizer gate

Status: failed architecture gate; no long training was started.

Command:

```text
julia --project=. --threads=20,0 \
  experiments/beat_first_v1/reduced_hay_cpu_native/overfit_candidate_delta_dendritic.jl \
  --states 64 --updates 200 --log-every 50 --workers 20 \
  --learning-rate 0.003 --warmup-updates 0 \
  --min-learning-rate-ratio 1 --clip-norm 2
```

This matches the successful flat-control optimizer scale (`LR=0.003`, no
learning-rate decay, global clip 2) instead of the earlier DDF cosine schedule.

| update | excess | tie-aware top-1 |
|---:|---:|---:|
| 0 | 4.127599 | 0.031250 |
| 50 | 3.172652 | 0.078125 |
| 100 | 2.450943 | 0.203125 |
| 150 | 2.103760 | 0.250000 |
| 200 | 1.900604 | 0.328125 |

Training-only throughput was `10.366 updates/s` and `663.453 states/s` after
JIT and data loading.  Every hard-event counter remained exactly zero at all
reported updates:

```text
leaf_events     0
internal_events 0
root_events     0
output_events   0
```

The result falsifies the resting-state E/I calibration currently represented
by `EXCITATORY_DRIVE_SCALE = 0.035`.  That calibration equalized a small
positive AMPA/NMDA response with a small negative GABA response around rest,
but moved the canonical workload far below the NMDA/plateau/spike operating
region.  It also confirms that optimizer scale alone does not explain the DDF
regression.

No 1k, 3k, or 100k continuation is authorized from this configuration.
